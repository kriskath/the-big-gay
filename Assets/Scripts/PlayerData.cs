using UnityEngine;

[System.Serializable] 
// Marks a class as serializable for data storage and Unity Inspector exposure.
// I.e. converts a class instance into a JSON (or binary) representation

public class PlayerData
{
    /* 
    Add the fields you'd like to save
    public int playerLevel;
    public float playerHealth;
    public Vector3 playerPosition;
    */

    /* Class Constructors */
}
